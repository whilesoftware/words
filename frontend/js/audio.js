var words = angular.module('words);

words.factory(
  'Audio',
  [
    '$rootScope',
    '$timeout',
    '$window',
    function($rootScope, $timeout, $window) {
      var self = {};
      return self;
    }
  ]
);
